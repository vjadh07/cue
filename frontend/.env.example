# Copy to .env.local for a custom setup. Not needed for local dev — the
# default already points at the local backend.

# The Cue backend the studio talks to. Leave unset for local dev
# (defaults to http://localhost:8000); set to your deployed backend in prod.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
