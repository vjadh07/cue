# Copy this file to .env and paste your real key after the = sign.
# Get a free key at https://elevenlabs.io  (Profile menu -> API key)
# .env is git-ignored, so your key never gets committed.
ELEVENLABS_API_KEY=
